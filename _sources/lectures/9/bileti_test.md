# Пример билетов для экзамена
<p style="page-break-after:always;"></p>

1. Напишите короткий пример использования конструкции `if else`.
2. 
```python 
list(range(-2, 5, 2))
```
3. Чем tuple отличается от list?
<p style="page-break-after:always;"></p>

1. Напишите короткий пример использования конструкции `if elif`.
2. 
```
[i for i in range(10) if i % 2 == 0]
```
3. Что такое генератор списков(list comprehension)?
<p style="page-break-after:always;"></p>

1. Напишите короткий пример использования конструкции `for`.
2. 
```
“Hi ” * 3
```
3. Как сделать проверку на тип данных?
<p style="page-break-after:always;"></p>

1. Напишите короткий пример создания функции.
2. 
```
*list(‘Hello’)
```
3. Что такое параметры по умолчанию?
<p style="page-break-after:always;"></p>

1. Напишите короткий пример создания списков.
2. 
```python
l = []
for i in range(10):
	if i % 3 == 0:
		l.append(i)
print(l)
```
3. Какие ограничения на ключи в словарях? А на значениях?
<p style="page-break-after:always;"></p>

1. Напишите короткий пример создания словарей.
2. 
```python
dict(k2 = 1, k1 = 2)
```
3. Что делает функция enumerate?
<p style="page-break-after:always;"></p>
